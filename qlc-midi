#!/usr/bin/env python3
"""Entry point wrapper so the tool runs from a checkout without installing."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

from qlcprofiler.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
